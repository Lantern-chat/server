{
    "private": true,
    "version": "0.0.0",
    "scripts": {
        "deploy": "wrangler publish",
        "dev": "wrangler dev --local",
        "build": "wrangler build"
    },
    "devDependencies": {
        "wrangler": "^2.0.0"
    }
}